# 1.5 m/s, curvature window 1.2 m: braking starts earlier than at 0.8 - #9.
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 1.5
      path_curvature_lookahead_dist: 1.2

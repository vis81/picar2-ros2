# 1.0 m/s with a 2.0 m approach ramp: must not touch lap time now that the
# ramp only applies once the goal is inside the local window (#2).
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 1.0
      approach_velocity_scaling_dist: 2.0

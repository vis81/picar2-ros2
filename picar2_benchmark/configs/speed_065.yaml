# 0.65 m/s — the speed the robot's F1 route runs at, so the sim's warning
# profile can be compared against the hardware log rather than only its times.
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 0.65

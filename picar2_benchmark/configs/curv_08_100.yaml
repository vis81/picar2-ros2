# picar2 RPP fork: regulate on the sharpest bend of the path within 0.8 m
# ahead, so the slow-down starts before a corner instead of inside it.
controller_server:
  ros__parameters:
    FollowPath:
      desired_linear_vel: 1.0
      path_curvature_lookahead_dist: 0.8
